# 天翔ビルディング ビル状態管理ポータル　要件定義書

**作成日：** 2026年4月6日  
**バージョン：** 1.0  
**ステータス：** 確定

---

## 1. プロジェクト概要

### 目的
各ビルの状態を社員・入居者がリアルタイムで共有し、過去の状態変化履歴をもとにLLMが次に起こりうる事象を予測・アドバイスするポータルシステムを構築する。

### 展示用スコープ
- 対象ビル：2〜3棟
- デモ用サンプルデータ：数十件
- 認証：なし（展示用・画面切り替えのみ）

---

## 2. ユーザー定義

| ユーザー種別 | 権限 | 端末 |
|---|---|---|
| 社員 | 入力・編集・閲覧・管理（全機能） | PC 1台 + スマホ 1台 |
| 入居者 | 閲覧のみ（読み取り専用） | PC + スマホ |

- 管理者／一般の権限区別：**なし**
- ログイン認証：**なし**（社員・入居者は画面URLで切り替え）

---

## 3. 機能要件

### コア機能一覧

| # | 機能名 | 内容 |
|---|---|---|
| F-01 | 状態表示 | 設備・清掃・インシデント・入退館の4カテゴリを一覧表示 |
| F-02 | 状態入力 | 「正常／異常／対応中」3択ステータスで更新 |
| F-03 | 履歴管理 | 状態変化をすべてDBに保存（変更日時・メモ付き） |
| F-04 | LLM予測エージェント | 「予測を見る」ボタン押下でアドバイスを表示（**メイン機能**） |
| F-05 | 社員向け画面 | 入力・管理・閲覧・履歴・予測の全機能 |
| F-06 | 入居者向け画面 | 現在の状態のみシンプル表示（別画面） |

### 状態カテゴリ詳細

| カテゴリ | 管理項目（例） |
|---|---|
| 設備系 | エレベーター・空調・電気・水道 |
| 清掃系 | 各フロア清掃状況 |
| インシデント系 | クレーム対応中・修繕依頼中 |
| 入退館系 | 施錠状態・来訪者対応中 |

### LLM予測エージェント仕様（F-04）

- **入力：** 同ビル・同カテゴリの過去履歴（直近N件）+ 現在のステータス + メモ
- **処理：** Anthropic APIへ履歴をコンテキストとして渡し、次に起こりうる事象を予測
- **出力：** 予測される事象とその対応アドバイスをポータル上に表示
- **トリガー：** 「予測を見る」ボタン押下（手動）

---

## 4. 非機能要件

| 項目 | 内容 |
|---|---|
| 認証 | なし（展示用） |
| データ量 | デモ用サンプルデータ数十件 |
| ネットワーク | インターネット接続前提 |
| コスト | 無料枠での運用を最優先 |
| レスポンシブ | PC・スマホ両対応 |

---

## 5. 画面構成

### 社員向け画面

```
/ （トップ）
└── ビル選択画面
      ↓
/buildings/[id]
└── ビル詳細画面
      ├── 現在の状態一覧（4カテゴリ）
      ├── 状態を更新するボタン
      ├── 履歴一覧
      └── 「予測を見る」ボタン
            ↓
            LLMアドバイス表示エリア
```

### 入居者向け画面

```
/resident （トップ）
└── ビル選択画面
      ↓
/resident/buildings/[id]
└── ビル状態画面
      └── 現在の状態一覧（シンプル表示・読み取り専用）
```

---

## 6. データ設計

### テーブル一覧

#### buildings（ビルマスタ）
| カラム名 | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| name | TEXT | ビル名 |

#### statuses（現在の状態）
| カラム名 | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| building_id | UUID | ビルID（FK） |
| category | TEXT | 設備／清掃／インシデント／入退館 |
| item | TEXT | エレベーターなど |
| status | TEXT | 正常／異常／対応中 |
| updated_at | TIMESTAMP | 更新日時 |

#### status_history（状態変化履歴）← **メインテーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| building_id | UUID | ビルID（FK） |
| category | TEXT | カテゴリ |
| item | TEXT | 管理項目 |
| status | TEXT | 正常／異常／対応中 |
| changed_at | TIMESTAMP | 変更日時 |
| memo | TEXT | 状況メモ（LLM精度向上に重要） |

> **設計方針：** status_historyはLLMへのコンテキストとして直接使用するため、memoの入力を推奨する。過去履歴の質がエージェント精度に直結する。

---

## 7. 技術スタック

| レイヤー | 技術 | 選定理由 |
|---|---|---|
| フロントエンド | **Next.js** | 社員・入居者の画面分離が容易、Vercel無料枠で運用可 |
| データベース | **Supabase（PostgreSQL）** | リアルタイム同期・無料枠・認証不要でも使用可 |
| LLMエージェント | **Python + FastAPI** | 既習構成、Anthropic API連携が容易 |
| LLM | **Anthropic API（Claude）** | 日本語精度・コンテキスト処理に優れる |
| ホスティング（FE） | **Vercel** | Next.jsとの親和性・無料枠 |
| ホスティング（API） | **Render** | FastAPI無料枠運用可 |

---

## 8. 開発フェーズ

### Phase 1：状態共有ポータル（土台）
- ビル・状態テーブルのDB構築（Supabase）
- 社員向け・入居者向け画面の実装（Next.js）
- 状態入力・表示・リアルタイム同期

### Phase 2：履歴管理
- status_historyテーブルの実装
- 状態変化時の自動履歴登録
- 履歴一覧表示・メモ入力UI
- デモ用サンプルデータ投入

### Phase 3：LLM予測エージェント（メイン）
- FastAPI + Anthropic API 連携
- 過去履歴取得→LLMコンテキスト構築→予測生成
- 「予測を見る」ボタン連携・アドバイス表示UI

---

## 9. 未決定事項（今後の検討）

| 項目 | 内容 |
|---|---|
| サンプルデータ内容 | どのビル名・どの事象履歴を用意するか |
| LLMプロンプト設計 | 予測の精度を左右するため別途設計が必要 |
| 本番化時の認証方式 | 展示後に社内展開する場合はSupabase Authを追加 |

---

*本ドキュメントはCursorプロジェクトルートに `requirements.md` として配置し、Claude Codeが常時参照できる状態にすること。*
